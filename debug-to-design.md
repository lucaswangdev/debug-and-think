# Bug 排查工程师的快速定位方法论

## 核心思路：先理解，再动手

```
复现 → 定位 → 验证 → 修复 → 回归
```

-----

## 第一步：快速建立上下文（5分钟内）

**不要直接看代码**，先问清楚：

- 什么现象？报错信息、截图、日志
- 什么时候开始出现的？有没有最近的变更（上线、配置、数据）
- 必现还是偶现？什么条件下触发？
- 影响范围：所有用户还是部分？

> **关键：Bug 的边界条件往往就是答案所在。**

-----

## 第二步：复现问题

- 本地能复现 → 最理想，可以加断点
- 只在线上复现 → 靠日志 + 监控
- 偶现 → 先找规律（时间、用户、数据特征）

**复现不了，不要乱改代码。**

-----

## 第三步：定位（二分法思维）

```
系统入口 → 中间层 → 数据层
```

逐层排查，快速缩小范围：

|层级|看什么                 |
|--|--------------------|
|日志|ERROR/WARN，时间戳对齐，上下文|
|网络|接口返回值，超时，状态码        |
|数据|DB 数据是否异常，有没有脏数据    |
|代码|入参/出参，边界条件，空值判断     |
|环境|配置差异，依赖版本，资源限制      |

**善用工具**：`grep`、`jq`、`curl`、抓包、`explain` SQL，比盯代码快 10 倍。

-----

## 第四步：读代码的正确方式

不要从头读，**顺着调用链找关键路径**：

1. 从报错堆栈 / 日志入口往上追
1. 重点看：**条件判断、异常处理、并发操作、外部调用**
1. 怀疑哪里，加临时日志或断点验证，不要凭感觉猜

```python
# 比如看到 NullPointerException，立刻找：
# 这个对象从哪来？什么情况下为 null？调用方有没有判空？
```

-----

## 第五步：修复原则

- **最小改动**：只动有问题的地方，不顺手重构
- **先写验证**：改完先跑原来的复现路径
- **记录原因**：注释写清楚"为什么这么改"，不只是"改了什么"
- **回归测试**：想想这个改动会不会影响其他路径

-----

## 高频 Bug 模式速查

```
空指针/空值    → 入参校验、默认值、Optional
并发问题       → 锁、原子操作、共享状态
超时/性能      → 慢查询、N+1、连接池耗尽
数据不一致     → 事务边界、幂等性、缓存失效
配置/环境差异  → 环境变量、feature flag、版本不一致
```

-----

## 一句话总结

> **数据驱动，二分定位，最小修复，验证回归。**  
> 靠证据说话，不靠直觉猜测。